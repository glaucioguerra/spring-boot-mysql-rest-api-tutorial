version: '3'
services:
  mariadb:
    image: "mariadb:latest"
    ports:
      - "3306:3306"
    volumes:
      - /root/docker/mariadb/datadir:/var/lib/mysql
  app:
    build: .
    ports:
      - "8090:8080"